apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend-chart.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontend-chart.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never
